# Local Environment Variables (for local development only)
# Copy this file to .env.local and update with your actual values
# This file is for local overrides and should never be committed

# Override any environment variables for local development here
# These will take precedence over .env.development

# Example local overrides:
# DATABASE_URL=postgresql://localhost:5432/my_local_db
# API_BASE_URL=http://localhost:4000/api
# DISABLE_AUTH=true
# MOCK_EXTERNAL_SERVICES=true
